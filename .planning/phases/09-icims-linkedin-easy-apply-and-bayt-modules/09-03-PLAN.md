---
phase: 09-icims-linkedin-easy-apply-and-bayt-modules
plan: 03
type: tdd
wave: 2
depends_on:
  - 09-01
files_modified:
  - platforms/linkedin.js
  - tests/unit/linkedin.test.js
autonomous: true
requirements:
  - FR-2.2
  - FR-2.3
  - FR-7.3

must_haves:
  truths:
    - "linkedin.js registers on window.JobFill.platforms.linkedin"
    - "matches() returns true for linkedin.com"
    - "fill() returns empty array (not error) when .jobs-easy-apply-modal is absent"
    - "fill() fills phone, email, firstName, lastName inside the modal"
    - "fill() NEVER clicks Submit or Next buttons"
    - "fill() stores MutationObserver on window._jobfillLinkedInObserver"
    - "fill() uses per-field async delay (sleep) between fills"
    - "All 7 linkedin.test.js tests are GREEN"
  artifacts:
    - path: "platforms/linkedin.js"
      provides: "LinkedIn Easy Apply platform module IIFE"
      exports: ["matches", "fill", "getJobDetails"]
      min_lines: 160
    - path: "tests/unit/linkedin.test.js"
      provides: "7 passing GREEN tests (no todo)"
  key_links:
    - from: "platforms/linkedin.js"
      to: ".jobs-easy-apply-modal"
      via: "isEasyApplyContext() check at top of fill()"
      pattern: "jobs-easy-apply-modal"
    - from: "platforms/linkedin.js"
      to: "window._jobfillLinkedInObserver"
      via: "assigned in fill() after initial field fill"
      pattern: "_jobfillLinkedInObserver"
    - from: "platforms/linkedin.js"
      to: "window.JobFill.filler.fillField"
      via: "called per field with await sleep() between each call"
      pattern: "fillField\\(el, value\\)"
---

<objective>
Implement `platforms/linkedin.js` using TDD: RED → GREEN → REFACTOR against the 7 stubs created in 09-01.

Purpose: LinkedIn Easy Apply has two constraints not present in any prior module — (1) per-field async delay to avoid bot detection, (2) a MutationObserver for multi-step modal navigation. The no-auto-submit constraint is a hard requirement (FR-7.3).
Output: platforms/linkedin.js (IIFE, ~160–220 lines), all 7 linkedin tests GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-RESEARCH.md
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-01-SUMMARY.md

<!-- Canonical pattern to follow exactly -->
@platforms/greenhouse.js
@platforms/ashby.js
</context>

<interfaces>
<!-- window.JobFill primitives -->
window.JobFill.filler.fillField(el, value)   // returns boolean
window.JobFill.matcher.findBestAnswer(label, answerBank)
window.JobFill.matcher.substituteVariables(rawAnswer, vars)

<!-- LinkedIn field selectors — ALL scoped inside .jobs-easy-apply-modal (RESEARCH.md) -->
var MODAL_SCOPE = '.jobs-easy-apply-modal ';
var FIELDS = [
  { label: 'Phone',     profileKey: 'phone',      selectors: [MODAL_SCOPE + 'input[id*="phoneNumber"]', MODAL_SCOPE + 'input[type="tel"]'] },
  { label: 'Email',     profileKey: 'email',       selectors: [MODAL_SCOPE + 'input[id*="email"]',       MODAL_SCOPE + 'input[type="email"]'] },
  { label: 'First Name',profileKey: 'firstName',   selectors: [MODAL_SCOPE + 'input[id*="firstName"]',   MODAL_SCOPE + 'input[autocomplete="given-name"]'] },
  { label: 'Last Name', profileKey: 'lastName',    selectors: [MODAL_SCOPE + 'input[id*="lastName"]',    MODAL_SCOPE + 'input[autocomplete="family-name"]'] },
];

<!-- sleep helper — inline one-liner, no utility needed (RESEARCH.md dont_hand_roll) -->
function sleep(ms) { return new Promise(function(resolve) { setTimeout(resolve, ms); }); }

<!-- MutationObserver pattern (RESEARCH.md pattern 2) -->
// After initial fill, observe modal for step changes:
// var modal = document.querySelector('.jobs-easy-apply-modal');
// if (modal) {
//   var observer = new MutationObserver(function() { fillStandardFields(profile, [], new Set()); });
//   observer.observe(modal, { childList: true, subtree: true });
//   window._jobfillLinkedInObserver = observer;  // stored for content.js to disconnect
// }

<!-- CRITICAL: fill() must NEVER call .click() on any button element -->
<!-- Specifically forbidden: Submit application button, Continue to next step button -->
</interfaces>

<feature>
  <name>platforms/linkedin.js</name>
  <files>platforms/linkedin.js, tests/unit/linkedin.test.js</files>
  <behavior>
    - matches('www.linkedin.com') → true
    - matches('linkedin.com') → true
    - matches('greenhouse.io') → false
    - fill() when document has no .jobs-easy-apply-modal → returns [] (empty array, no error)
    - fill({phone:'555',...}, []) with modal DOM present → results include {field:'Phone', status:'filled'}
    - fill() with pre-filled input → {status:'skipped', reason:'already has value'}
    - After fill() with modal present → window._jobfillLinkedInObserver is a MutationObserver instance
    - fill() never calls .click() on any button (assert no click() calls on button elements)
  </behavior>
  <implementation>
    Follow greenhouse.js / ashby.js structure:
    1. IIFE header — window.JobFill namespace guard
    2. MODAL_SCOPE constant = '.jobs-easy-apply-modal '
    3. FIELDS table — 4 entries (phone, email, firstName, lastName) — all selectors prefixed with MODAL_SCOPE
    4. matches(hostname) — hostname.indexOf('linkedin.com') !== -1
    5. resolveSelector(selectors) — same as greenhouse.js
    6. hasValue(el) — same as greenhouse.js
    7. sleep(ms) — inline Promise/setTimeout one-liner
    8. isEasyApplyContext() — !!document.querySelector('.jobs-easy-apply-modal')
    9. getJobDetails() — parse document.title; LinkedIn pages: "Job Title at Company | LinkedIn"
    10. fillStandardFields(profile, results, handledEls) — async loop with await sleep(50 + Math.random()*150) after each fillField call
    11. fill(profile, answerBank) — async; isEasyApplyContext() guard returns [] if modal absent; then fillStandardFields; then MutationObserver setup; return results

    NOTE: fillCustomQuestions is NOT included for LinkedIn. The modal does not surface custom text questions in the same pattern; any textarea in the modal gets pushed as needs_review automatically via bare textarea scan (single-pass).

    RED commit: `test(09-03): convert linkedin stubs to failing assertions`
    GREEN commit: `feat(09-03): implement platforms/linkedin.js`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — convert linkedin.test.js stubs to failing assertions</name>
  <files>tests/unit/linkedin.test.js</files>
  <behavior>
    - Test 1: matches('www.linkedin.com') === true
    - Test 2: fill() with no modal → returns array with length 0
    - Test 3: fill() with modal DOM → phone result status === 'filled'
    - Test 4: fill() with modal DOM → firstName and lastName results present
    - Test 5: fill() never calls button.click() (track via spy on HTMLButtonElement.prototype.click)
    - Test 6: fill() with modal → window._jobfillLinkedInObserver instanceof MutationObserver
    - Test 7: fill() with pre-filled input → result status === 'skipped'
  </behavior>
  <action>
Replace the 7 todo stubs with real assertions. Remove `{ todo: '...' }` option.

For modal DOM tests: use dom-linkedin.html fixture seeded into document mock. The mock must handle querySelectorAll for button elements (test 5 needs button presence verifiable). Implement a click-tracking spy: override element.click on all button elements found in fixture, record calls, assert spy was never called after fill().

For MutationObserver test (test 6): the document mock's window must expose a real or mock `MutationObserver` constructor. A minimal mock that records `observe()` calls is sufficient. After fill() runs, assert `window._jobfillLinkedInObserver` is truthy (not null).

For sleep test: fill() is async — await it. The delay does NOT need to be verified for exact duration; presence of `await sleep(...)` in the implementation is sufficient. The test just confirms fill() is async and completes without error.

Run: `node --test tests/unit/linkedin.test.js` — must show 7 FAIL. That is RED.
  </action>
  <verify>node --test tests/unit/linkedin.test.js 2>&1 | grep -E "fail|pass|todo"</verify>
  <done>7 tests FAIL. 0 todo. Commit: `test(09-03): convert linkedin stubs to failing assertions`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement platforms/linkedin.js until all 7 tests pass</name>
  <files>platforms/linkedin.js</files>
  <behavior>
    - All 7 linkedin tests GREEN
    - Full test suite passes, 0 regressions
  </behavior>
  <action>
Create `platforms/linkedin.js` as an IIFE. Implement all sections listed in the `<feature><implementation>` block above.

Key implementation details:
- `matches(hostname)`: `return hostname.indexOf('linkedin.com') !== -1;`
- `isEasyApplyContext()`: `return !!document.querySelector('.jobs-easy-apply-modal');`
- `fill()` guard: if `!isEasyApplyContext()` return `[]` immediately — do NOT push any result, return empty array.
- `fillStandardFields()`: must be `async`. Loop over FIELDS, resolve selector, check hasValue, call fillField, push result, then `await sleep(50 + Math.random() * 150)` — the sleep comes AFTER the push, between fields.
- MutationObserver: after `fillStandardFields` completes, `var modal = document.querySelector('.jobs-easy-apply-modal'); if (modal) { var observer = new MutationObserver(function() { fillStandardFields(profile, [], new Set()); }); observer.observe(modal, { childList: true, subtree: true }); window._jobfillLinkedInObserver = observer; }`
- NEVER add `.click()` anywhere in the file. No button interactions of any kind.
- `getJobDetails()`: LinkedIn titles are "Job Title at Company | LinkedIn" — try regex `/^(.+?)\s+at\s+(.+?)\s*\|/i` on document.title. Fallback: split on ' | ' and take parts[0] as jobTitle.

Run `node --test tests/unit/linkedin.test.js` until 7/7 GREEN. Then full suite.
  </action>
  <verify>node --test tests/unit/linkedin.test.js 2>&1 | tail -3</verify>
  <done>7/7 linkedin tests pass. Full suite passes. platforms/linkedin.js exists (~160–220 lines). Commit: `feat(09-03): implement platforms/linkedin.js`</done>
</task>

</tasks>

<verification>
node --test tests/unit/linkedin.test.js
Expected: 7 pass, 0 fail, 0 todo

node --test tests/unit/
Expected: full suite passes, 0 regressions
</verification>

<success_criteria>
- platforms/linkedin.js is a valid IIFE registering on window.JobFill.platforms.linkedin
- matches(), fill(), getJobDetails() all exported
- fill() returns [] when modal absent — no error, no results
- fill() fills phone/email/firstName/lastName inside .jobs-easy-apply-modal with per-field delay
- fill() stores MutationObserver on window._jobfillLinkedInObserver
- fill() contains zero .click() calls on any element
- 7/7 linkedin.test.js GREEN, 0 fail, 0 todo
- Full test suite: 0 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-03-SUMMARY.md`
</output>
