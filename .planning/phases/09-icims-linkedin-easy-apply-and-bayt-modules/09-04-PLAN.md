---
phase: 09-icims-linkedin-easy-apply-and-bayt-modules
plan: 04
type: tdd
wave: 2
depends_on:
  - 09-01
files_modified:
  - platforms/bayt.js
  - tests/unit/bayt.test.js
autonomous: true
requirements:
  - FR-2.2
  - FR-2.3
  - FR-7.5

must_haves:
  truths:
    - "bayt.js registers on window.JobFill.platforms.bayt"
    - "matches() returns true for bayt.com"
    - "fill() uses ONLY name/id/type attribute selectors — no label text, no placeholder, no aria-label"
    - "fill() returns empty results when isNativeBaytForm() is false (ATS redirect guard)"
    - "fill() fills FirstName, LastName, Email, Phone using attribute selectors"
    - "fill() handles both PascalCase (FirstName) and snake_case (first_name) selector variants"
    - "All 6 bayt.test.js tests are GREEN"
  artifacts:
    - path: "platforms/bayt.js"
      provides: "Bayt platform module IIFE with RTL-safe attribute-only selectors"
      exports: ["matches", "fill", "getJobDetails"]
      min_lines: 130
    - path: "tests/unit/bayt.test.js"
      provides: "6 passing GREEN tests (no todo)"
  key_links:
    - from: "platforms/bayt.js"
      to: "FIELDS selector arrays"
      via: "selectors use ONLY [name=], [id*=], [type=], [autocomplete=] — zero label/placeholder/aria"
      pattern: "\\[name=|\\[id\\*=|\\[type=|\\[autocomplete="
    - from: "platforms/bayt.js"
      to: "isNativeBaytForm()"
      via: "called at start of fill() — returns early with [] if false"
      pattern: "isNativeBaytForm"
---

<objective>
Implement `platforms/bayt.js` using TDD: RED → GREEN → REFACTOR against the 6 stubs created in 09-01.

Purpose: Bayt is the RTL platform. The single architectural constraint is that ALL field identification must use `name`/`id`/`type` attribute selectors exclusively — no label text, no placeholder, no aria-label (FR-7.5). Arabic label text is present in the DOM but must never be used for matching.
Output: platforms/bayt.js (IIFE, ~130–180 lines), all 6 bayt tests GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-RESEARCH.md
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-01-SUMMARY.md

<!-- Canonical pattern to follow exactly -->
@platforms/greenhouse.js
@platforms/ashby.js
</context>

<interfaces>
<!-- window.JobFill primitives -->
window.JobFill.filler.fillField(el, value)   // returns boolean
window.JobFill.matcher.findBestAnswer(label, answerBank)
window.JobFill.matcher.substituteVariables(rawAnswer, vars)

<!-- Bayt FIELDS table — attribute-only selectors, PascalCase + snake_case variants (RESEARCH.md) -->
// CRITICAL: No selector may contain label text, placeholder text, or aria-label strings.
// All selectors must be one of: [name=...], [id*=...], [type=...], [autocomplete=...]
var FIELDS = [
  { label: 'First Name', profileKey: 'firstName',
    selectors: ['input[name="FirstName"]', 'input[name="first_name"]', 'input[id*="FirstName"]', 'input[autocomplete="given-name"]'] },
  { label: 'Last Name',  profileKey: 'lastName',
    selectors: ['input[name="LastName"]',  'input[name="last_name"]',  'input[id*="LastName"]',  'input[autocomplete="family-name"]'] },
  { label: 'Email',      profileKey: 'email',
    selectors: ['input[type="email"]',     'input[name="Email"]',      'input[name="email"]'] },
  { label: 'Phone',      profileKey: 'phone',
    selectors: ['input[type="tel"]',       'input[name="Phone"]',      'input[name="mobile"]'] },
];

<!-- isNativeBaytForm() guard (RESEARCH.md pattern 3) -->
// Returns false if Bayt has redirected to a third-party ATS — other platform module handles that case
function isNativeBaytForm() {
  return !!document.querySelector('form[action*="bayt.com"], form[id*="apply"], #applyForm');
}

<!-- fillCustomQuestions: SKIP for Bayt -->
// Custom questions on Bayt use Arabic question text — label matching is unreliable.
// All textarea elements → push needs_review, do NOT attempt answer bank lookup.
// This is intentional and correct per FR-7.5.
</interfaces>

<feature>
  <name>platforms/bayt.js</name>
  <files>platforms/bayt.js, tests/unit/bayt.test.js</files>
  <behavior>
    - matches('www.bayt.com') → true
    - matches('bayt.com') → true
    - matches('greenhouse.io') → false
    - fill() with document that has no bayt.com form signature → returns [] (isNativeBaytForm false)
    - fill({firstName:'Jane',...}, []) with dom-bayt.html fixture → results include {field:'First Name', status:'filled'}
    - fill() with dom-bayt.html fixture → result for Email present (type="email" selector works)
    - fill() with dom-bayt.html fixture → result for Phone present (type="tel" or name="Phone" selector)
    - NO selector in FIELDS array contains label text, placeholder text, or aria-label string
    - textarea in fixture → pushed as needs_review (not looked up in answer bank)
  </behavior>
  <implementation>
    Follow greenhouse.js / ashby.js structure:
    1. IIFE header — window.JobFill namespace guard
    2. FIELDS table — 4 entries (firstName, lastName, email, phone) — attribute selectors ONLY
    3. matches(hostname) — hostname.indexOf('bayt.com') !== -1
    4. resolveSelector(selectors) — same as greenhouse.js
    5. hasValue(el) — same as greenhouse.js
    6. isNativeBaytForm() — querySelector check on form signature
    7. getJobDetails() — Bayt title pattern: "Job Title - Company | Bayt.com", split on ' - ' and ' | '
    8. fillStandardFields(profile, results, handledEls) — same loop as greenhouse.js
    9. fillCustomQuestions(answerBank, jobDetails, results, handledEls):
       - Query all textarea elements
       - Skip handledEls
       - For each unhandled textarea: push { field: el.name || el.id || 'Custom Question', status: 'needs_review', reason: 'Bayt RTL — manual review required' }
       - Do NOT call findBestAnswer (Arabic text would fail matching)
    10. fill(profile, answerBank) — async; isNativeBaytForm() guard first → return [] if false; then standard two-pass fill
    11. return { matches, fill, getJobDetails }

    NOTE: getAdjacentLabel() is intentionally OMITTED from bayt.js. The function exists in greenhouse/ashby to extract label text — on Bayt, label text is Arabic and must never be used for matching. Omitting the function makes the constraint self-enforcing.

    RED commit: `test(09-04): convert bayt stubs to failing assertions`
    GREEN commit: `feat(09-04): implement platforms/bayt.js`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — convert bayt.test.js stubs to failing assertions</name>
  <files>tests/unit/bayt.test.js</files>
  <behavior>
    - Test 1: matches('www.bayt.com') === true
    - Test 2: fill() with no form signature → returns array with length 0
    - Test 3: fill() with dom-bayt.html → FirstName result status === 'filled'
    - Test 4: fill() with dom-bayt.html → Email result status === 'filled'
    - Test 5: FIELDS selector audit: scan all selector strings in module — assert none contain 'placeholder', 'aria-label', label-derived text strings (test reads platforms/bayt.js as text and scans for forbidden patterns)
    - Test 6: fill() with dom-bayt.html → Phone result status === 'filled'
  </behavior>
  <action>
Replace the 6 todo stubs with real assertions. Remove `{ todo: '...' }` option.

For DOM tests: use dom-bayt.html fixture. Mini document mock must handle querySelector with attribute selectors — the mock needs to parse `[name="FirstName"]` style selectors correctly since that is the core behavior under test.

For test 5 (RTL safety assertion): this test reads `platforms/bayt.js` as a string via `fs.readFileSync`, then asserts:
- The file content does NOT match `/placeholder\s*\*/i` in any selector string
- The file content does NOT match `/aria-label/i` in any selector string
- The file content does NOT contain Arabic Unicode characters in selector strings (optional but good to confirm)
This test will fail in RED because the file does not exist yet. In GREEN it confirms the RTL constraint is structurally enforced in the source.

For test 2 (isNativeBaytForm guard): set up document mock with no form elements (empty querySelector returns). fill() must return [].

Run: `node --test tests/unit/bayt.test.js` — must show 6 FAIL. That is RED.
  </action>
  <verify>node --test tests/unit/bayt.test.js 2>&1 | grep -E "fail|pass|todo"</verify>
  <done>6 tests FAIL. 0 todo. Commit: `test(09-04): convert bayt stubs to failing assertions`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement platforms/bayt.js until all 6 tests pass</name>
  <files>platforms/bayt.js</files>
  <behavior>
    - All 6 bayt tests GREEN
    - Full test suite passes, 0 regressions
  </behavior>
  <action>
Create `platforms/bayt.js` as an IIFE. Implement all sections listed in the `<feature><implementation>` block above.

Key implementation details:
- `matches(hostname)`: `return hostname.indexOf('bayt.com') !== -1;`
- `isNativeBaytForm()`: `return !!document.querySelector('form[action*="bayt.com"], form[id*="apply"], #applyForm');`
- `fill()` guard: if `!isNativeBaytForm()` return `[]` immediately — empty array, no results pushed.
- FIELDS: include BOTH PascalCase and snake_case selector variants per RESEARCH.md. No selector may reference label text.
- `getAdjacentLabel()`: DO NOT implement this function in bayt.js — its absence is intentional and enforces the RTL constraint.
- `fillCustomQuestions()`: scan all `textarea` elements, skip handledEls, push `{ field: el.name || el.id || 'Custom Question', status: 'needs_review', reason: 'Bayt RTL — manual review required' }` for each. Do NOT call `window.JobFill.matcher.findBestAnswer` anywhere.
- `getJobDetails()`: Bayt title format "Senior Marketer - Acme Corp | Bayt.com" — split on ` - ` first (parts[0]=jobTitle, parts[1] split on ' | ' gives companyName). Fallback: split on ` | `.

Run `node --test tests/unit/bayt.test.js` until 6/6 GREEN. Then full suite.
  </action>
  <verify>node --test tests/unit/bayt.test.js 2>&1 | tail -3</verify>
  <done>6/6 bayt tests pass. Full suite passes. platforms/bayt.js exists (~130–180 lines). Commit: `feat(09-04): implement platforms/bayt.js`</done>
</task>

</tasks>

<verification>
node --test tests/unit/bayt.test.js
Expected: 6 pass, 0 fail, 0 todo

node --test tests/unit/
Expected: full suite passes, 0 regressions
</verification>

<success_criteria>
- platforms/bayt.js is a valid IIFE registering on window.JobFill.platforms.bayt
- matches(), fill(), getJobDetails() all exported
- FIELDS table contains zero selectors referencing label text, placeholder, or aria-label
- getAdjacentLabel() is intentionally absent from bayt.js
- fill() guard: isNativeBaytForm() returns false → returns []
- fill() fills firstName, lastName, email, phone using attribute selectors
- textarea elements pushed as needs_review (no findBestAnswer call)
- 6/6 bayt.test.js GREEN, 0 fail, 0 todo
- Full test suite: 0 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-04-SUMMARY.md`
</output>
