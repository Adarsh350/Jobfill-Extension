---
phase: 09-icims-linkedin-easy-apply-and-bayt-modules
plan: 02
type: tdd
wave: 2
depends_on:
  - 09-01
files_modified:
  - platforms/icims.js
  - tests/unit/icims.test.js
autonomous: true
requirements:
  - FR-2.1
  - FR-2.3
  - FR-7.2

must_haves:
  truths:
    - "icims.js registers on window.JobFill.platforms.icims"
    - "matches() correctly identifies icims.com and careers.icims.com hostnames"
    - "fill() fills standard fields (firstName, lastName, email, phone) from profile"
    - "fill() detects cross-origin iframe and returns a failed result without throwing"
    - "fill() skips fields that already have a value (hasValue guard)"
    - "getJobDetails() extracts job title from .iCIMS_JobTitle element"
    - "All 7 icims.test.js tests are GREEN"
  artifacts:
    - path: "platforms/icims.js"
      provides: "iCIMS platform module IIFE"
      exports: ["matches", "fill", "getJobDetails"]
      min_lines: 150
    - path: "tests/unit/icims.test.js"
      provides: "7 passing GREEN tests (no todo)"
  key_links:
    - from: "platforms/icims.js"
      to: "window.JobFill.filler.fillField"
      via: "called in fillStandardFields for each matched element"
      pattern: "fillField\\(el, value\\)"
    - from: "platforms/icims.js"
      to: "window.top.location.href"
      via: "try/catch cross-origin detection in fill()"
      pattern: "window\\.top\\.location"
---

<objective>
Implement `platforms/icims.js` using TDD: RED → GREEN → REFACTOR against the 7 stubs created in 09-01.

Purpose: iCIMS is the most structurally distinct module — it runs inside an iframe (all_frames: true) and requires a cross-origin guard. The guard is the critical new behavior not seen in prior modules.
Output: platforms/icims.js (IIFE, ~150–200 lines), all 7 icims tests GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-RESEARCH.md
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-01-SUMMARY.md

<!-- Canonical pattern to follow exactly -->
@platforms/greenhouse.js
@platforms/ashby.js
</context>

<interfaces>
<!-- window.JobFill primitives — called at runtime, referenced by name (not cached at load) -->
window.JobFill.filler.fillField(el, value)   // returns boolean — true if filled
window.JobFill.matcher.findBestAnswer(label, answerBank)   // returns match object or null
window.JobFill.matcher.substituteVariables(rawAnswer, vars)  // returns resolved string

<!-- iCIMS field name conventions (both casing variants — RESEARCH.md pitfall 3) -->
// Standard fields with selector arrays covering both camelCase and lowercase:
// firstName: ['input[name="firstname"]', 'input[id*="FirstName"]', 'input[autocomplete="given-name"]']
// lastName:  ['input[name="lastname"]',  'input[id*="LastName"]',  'input[autocomplete="family-name"]']
// email:     ['input[type="email"]',     'input[name="email"]']
// phone:     ['input[type="tel"]',       'input[name="phone"]',    'input[name="phoneNumber"]']
// resume:    ['input[type="file"]',      'input[name*="resume" i]']  -- isFile: true, always skipped

<!-- Cross-origin detection (two-step — RESEARCH.md pitfall 1) -->
// Step 1: window === window.top → not in iframe at all → return false (no guard needed)
// Step 2: try { window.top.location.href; return false; } catch(e) { return true; }
// Cross-origin result: push { field: 'iCIMS Form', status: 'failed', reason: 'cross-origin iframe — manual fill required' }
// Also: chrome.runtime.sendMessage({ type: 'ICIMS_CROSS_ORIGIN' }) per ROADMAP spec

<!-- getJobDetails() for iCIMS (RESEARCH.md Code Examples) -->
// Primary: document.querySelector('.iCIMS_JobTitle, h1.iCIMS_Header')
// Company: try { window.parent.document.title.split(/[|\-\u2013]/)[1] } catch(e) {}
// Fallback: document.title split on | or -
</interfaces>

<feature>
  <name>platforms/icims.js</name>
  <files>platforms/icims.js, tests/unit/icims.test.js</files>
  <behavior>
    - matches('careers.icims.com') → true
    - matches('icims.com') → true
    - matches('greenhouse.io') → false
    - fill({firstName:'Jane',...}, []) with cross-origin mock (top.location throws) → returns [{field:'iCIMS Form', status:'failed', reason includes 'cross-origin'}]
    - fill({firstName:'Jane', lastName:'Doe', email:'j@d.com', phone:'555'}, []) with dom-icims.html fixture → results include filled entries for firstName, lastName, email, phone
    - fill() with pre-filled inputs (hasValue returns true) → status: 'skipped', reason: 'already has value'
    - getJobDetails() with dom-icims.html fixture → { jobTitle: 'Senior Marketing Manager', companyName: 'Acme Corp' }
  </behavior>
  <implementation>
    Follow greenhouse.js / ashby.js structure exactly:
    1. IIFE header — window.JobFill namespace guard
    2. FIELDS table — 5 entries (firstName, lastName, email, phone, resume[isFile])
    3. matches(hostname) — hostname.includes('icims.com')
    4. resolveSelector(selectors) — same as greenhouse.js
    5. hasValue(el) — same as greenhouse.js
    6. getAdjacentLabel(el) — same as greenhouse.js
    7. detectCrossOrigin() — two-step check per RESEARCH.md pattern 1
    8. getJobDetails() — .iCIMS_JobTitle primary, parent.document.title fallback, document.title fallback
    9. fillStandardFields(profile, results, handledEls) — same loop as greenhouse.js
    10. fillCustomQuestions(answerBank, jobDetails, results, handledEls) — same as greenhouse.js
    11. fill(profile, answerBank) — async, detectCrossOrigin() guard FIRST, then standard two-pass fill
    12. return { matches, fill, getJobDetails }

    RED commit: `test(09-02): convert icims stubs to failing assertions`
    GREEN commit: `feat(09-02): implement platforms/icims.js`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — convert icims.test.js stubs to failing assertions</name>
  <files>tests/unit/icims.test.js</files>
  <behavior>
    - Test 1: matches('careers.icims.com') === true
    - Test 2: matches('icims.com') === true
    - Test 3: matches('greenhouse.io') === false
    - Test 4: fill() cross-origin → results[0].status === 'failed', reason includes 'cross-origin'
    - Test 5: fill() with fixture DOM → at least firstName result has status 'filled'
    - Test 6: fill() with pre-filled input → result status 'skipped', reason 'already has value'
    - Test 7: getJobDetails() → jobTitle === 'Senior Marketing Manager'
  </behavior>
  <action>
Replace the 7 todo stubs with real assertions. Remove `{ todo: '...' }` option — use real assert calls.

For tests needing DOM: use the fixture HTML loaded in Task 2 of 09-01 (dom-icims.html) seeded into a mini document mock. The document mock must implement: querySelector, querySelectorAll, title (get/set). Each test that exercises fill() must set up window.JobFill mock with fillField stub returning true.

For cross-origin test (test 4): mock `window.top` to throw SecurityError when `.location` is accessed. The module under test calls `window.top.location.href` inside a try/catch — simulate by defining `window.top` as a getter that throws before the module code runs.

For getJobDetails test (test 7): set document.querySelector('.iCIMS_JobTitle') to return a mock element with textContent 'Senior Marketing Manager'. Set document.title to 'Senior Marketing Manager | Acme Corp'.

Run: `node --test tests/unit/icims.test.js` — must show 7 FAIL (not todo, not pass). That is the RED state.
  </action>
  <verify>node --test tests/unit/icims.test.js 2>&1 | grep -E "fail|pass|todo"</verify>
  <done>7 tests FAIL (assertions written, module not yet implemented). 0 todo. Commit: `test(09-02): convert icims stubs to failing assertions`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement platforms/icims.js until all 7 tests pass</name>
  <files>platforms/icims.js</files>
  <behavior>
    - All 7 icims tests GREEN
    - Full test suite (node --test on all unit files) still passes
  </behavior>
  <action>
Create `platforms/icims.js` as an IIFE following the greenhouse.js / ashby.js pattern exactly. Implement all sections listed in the `<feature><implementation>` block above.

Key implementation details:
- `matches(hostname)`: `return hostname.indexOf('icims.com') !== -1;`
- `detectCrossOrigin()`: step 1 check `window === window.top` → false. Step 2 try `window.top.location.href` → success=false, SecurityError=true.
- `fill()`: if `detectCrossOrigin()` returns true → push failed result + `try { chrome.runtime.sendMessage({type:'ICIMS_CROSS_ORIGIN'}); } catch(e) {}` → return results early. Do NOT proceed to fillStandardFields.
- `getJobDetails()`: try `.iCIMS_JobTitle, h1.iCIMS_Header` → try `window.parent.document.title` (wrapped in try/catch for cross-origin) → fallback `document.title` split on `/[|\-\u2013]/`.
- `fillCustomQuestions()`: standard textarea + input[type="text"] scan using getAdjacentLabel (same as greenhouse.js).
- File upload field (resume): always push `{ status: 'skipped', reason: 'resume upload in Phase 11' }` — consistent with Phase 07 decision.

Run `node --test tests/unit/icims.test.js` after each attempt until 7/7 GREEN.
Then run full suite to confirm no regressions.
  </action>
  <verify>node --test tests/unit/icims.test.js 2>&1 | tail -3</verify>
  <done>7/7 icims tests pass. Full suite passes. platforms/icims.js exists (~150–200 lines). Commit: `feat(09-02): implement platforms/icims.js`</done>
</task>

</tasks>

<verification>
node --test tests/unit/icims.test.js
Expected: 7 pass, 0 fail, 0 todo

node --test tests/unit/
Expected: full suite passes, 0 regressions
</verification>

<success_criteria>
- platforms/icims.js is a valid IIFE registering on window.JobFill.platforms.icims
- matches(), fill(), getJobDetails() all exported
- cross-origin detection: two-step check, sends ICIMS_CROSS_ORIGIN message, returns failed result
- fill() standard fields: firstName, lastName, email, phone fill correctly; resume skipped
- hasValue guard prevents re-filling already-filled inputs
- 7/7 icims.test.js GREEN, 0 fail, 0 todo
- Full test suite: 0 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-02-SUMMARY.md`
</output>
