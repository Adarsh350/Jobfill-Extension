---
phase: 09-icims-linkedin-easy-apply-and-bayt-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/dom-icims.html
  - tests/fixtures/dom-linkedin.html
  - tests/fixtures/dom-bayt.html
  - tests/unit/icims.test.js
  - tests/unit/linkedin.test.js
  - tests/unit/bayt.test.js
autonomous: true
requirements:
  - FR-2.1
  - FR-2.2
  - FR-2.3
  - FR-7.2
  - FR-7.3
  - FR-7.5

must_haves:
  truths:
    - "All three test files run without errors (todo stubs pass)"
    - "Each test file has anchors for every required behavior before implementation begins"
    - "DOM fixtures match the real platform structure each module will query"
  artifacts:
    - path: "tests/fixtures/dom-icims.html"
      provides: "iCIMS iframe form HTML fixture with standard field names"
    - path: "tests/fixtures/dom-linkedin.html"
      provides: "LinkedIn Easy Apply modal HTML fixture with .jobs-easy-apply-modal"
    - path: "tests/fixtures/dom-bayt.html"
      provides: "Bayt native form HTML fixture with PascalCase + snake_case name attributes"
    - path: "tests/unit/icims.test.js"
      provides: "Todo stubs for FR-2.1, FR-2.3, FR-7.2 (7 stubs)"
    - path: "tests/unit/linkedin.test.js"
      provides: "Todo stubs for FR-7.3 modal detection, fill, delay, no-submit (7 stubs)"
    - path: "tests/unit/bayt.test.js"
      provides: "Todo stubs for FR-7.5 attribute-only selectors, RTL safety (6 stubs)"
  key_links:
    - from: "tests/unit/icims.test.js"
      to: "tests/fixtures/dom-icims.html"
      via: "fs.readFileSync + document mock innerHTML"
      pattern: "dom-icims\\.html"
    - from: "tests/unit/linkedin.test.js"
      to: "tests/fixtures/dom-linkedin.html"
      via: "fs.readFileSync + document mock innerHTML"
      pattern: "dom-linkedin\\.html"
    - from: "tests/unit/bayt.test.js"
      to: "tests/fixtures/dom-bayt.html"
      via: "fs.readFileSync + document mock innerHTML"
      pattern: "dom-bayt\\.html"
---

<objective>
Create DOM fixtures (HTML files) and test stub files for all three Phase 9 platform modules: iCIMS, LinkedIn Easy Apply, and Bayt.

Purpose: Wave 1 scaffold that Wave 2 TDD plans (09-02, 09-03, 09-04) depend on. Tests must exist and pass as todo stubs before implementation begins — this is the RED anchor that makes TDD possible.
Output: 3 HTML fixtures, 3 test stub files. All stubs run green (todo-annotated) before any platform JS is written.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-RESEARCH.md

<!-- Prior phase scaffold pattern — how dom-greenhouse.html and greenhouse.test.js were built -->
@.planning/phases/07-greenhouse-and-lever-platform-modules/07-01-PLAN.md
</context>

<interfaces>
<!-- Test infrastructure pattern established in Phase 4 and reused in Phase 7/8 -->
<!-- All test files in this project use Node's built-in node:test module — NO Jest, NO jsdom, NO npm -->
<!-- Pattern from tests/unit/greenhouse.test.js and tests/unit/ashby.test.js: -->

// Self-contained mini DOM parser (no external deps):
const { readFileSync } = require('fs');
const { join } = require('path');

// document mock built per-test-file (not shared dom-mock.js) — Phase 07-03 decision
// Each file builds its own lightweight document object:
const _html = readFileSync(join(__dirname, '../fixtures/dom-PLATFORM.html'), 'utf8');
function makeDoc(html) {
  // ... mini querySelector mock seeded with html string
}
let document;

// Module load pattern (CJS shim):
let platform;
try {
  const { createRequire } = require('module');
  const req = createRequire(__filename);  // __filename not import.meta.url — CJS-only project
  platform = req('../../platforms/PLATFORM.js');
  // platform registers on window.JobFill.platforms.PLATFORM
} catch (e) { /* file not yet created — stubs still run */ }

// Stub pattern:
const { test, describe } = require('node:test');
test('TEST: matches() returns true for PLATFORM hostname', { todo: 'implement PLATFORM.js' }, () => {});
// todo stubs show as passing with annotation — keeps CI green, requirement coverage visible
// (Phase 4-01 decision)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create DOM fixtures for all 3 platforms</name>
  <files>
    tests/fixtures/dom-icims.html
    tests/fixtures/dom-linkedin.html
    tests/fixtures/dom-bayt.html
  </files>
  <action>
Create three minimal HTML fixture files representing the real DOM each platform module will query. Each must contain exactly the elements the test stubs and later TDD tests will assert against.

**dom-icims.html** — iCIMS iframe application form:
- `<div class="iCIMS_JobTitle">Senior Marketing Manager</div>`
- `<h1 class="iCIMS_Header">Senior Marketing Manager</h1>`
- Standard form fields using iCIMS naming conventions (cover BOTH camelCase and lowercase variants per RESEARCH.md pitfall 3):
  - `<input name="firstname" autocomplete="given-name">`
  - `<input name="lastname" autocomplete="family-name">`
  - `<input type="email" name="email">`
  - `<input type="tel" name="phone">`
  - `<input type="file" name="resume">`
- Wrap in `<form id="iCIMS_MainContent">`
- `<title>Senior Marketing Manager | Acme Corp</title>`

**dom-linkedin.html** — LinkedIn Easy Apply modal:
- Outer page wrapper: `<div class="jobs-search-results-container">`
- Modal: `<div class="jobs-easy-apply-modal">`
- Inside modal, step 1 fields scoped with LinkedIn id patterns:
  - `<input id="phoneNumber-nationalNumber" type="tel">`
  - `<input id="email-address" type="email">`
  - `<input id="firstName" autocomplete="given-name">`
  - `<input id="lastName" autocomplete="family-name">`
- Submit button (must NOT be clicked): `<button aria-label="Submit application">Submit application</button>`
- Next button (must NOT be clicked): `<button aria-label="Continue to next step">Next</button>`
- `<title>LinkedIn Job Search</title>`

**dom-bayt.html** — Bayt native application form:
- `<form id="applyForm" action="https://www.bayt.com/en/apply/">`
- Fields using PascalCase names (Bayt .NET backend per RESEARCH.md pitfall 3):
  - `<input name="FirstName" id="FirstName">`
  - `<input name="LastName" id="LastName">`
  - `<input type="email" name="Email" id="Email">`
  - `<input type="tel" name="Phone" id="Phone">`
  - `<textarea name="CoverLetter" id="CoverLetter"></textarea>`
- Also include snake_case variants as hidden extras so selector fallback coverage is verifiable:
  - `<!-- snake_case variants not present in Bayt DOM — PascalCase is authoritative -->`
- Arabic label text to represent RTL context (labels must NOT be relied upon):
  - `<label for="FirstName">الاسم الأول</label>`
  - `<label for="LastName">الاسم الأخير</label>`
- `<title>تقديم طلب | Bayt.com</title>`

Create `tests/fixtures/` directory if it does not exist.
  </action>
  <verify>
node -e "const fs = require('fs'); ['dom-icims','dom-linkedin','dom-bayt'].forEach(f => { const p = 'tests/fixtures/'+f+'.html'; if(!fs.existsSync(p)) throw new Error('missing: '+p); console.log('OK:', p); });"
  </verify>
  <done>3 HTML fixture files exist. dom-icims.html has iCIMS_JobTitle + iCIMS_MainContent form. dom-linkedin.html has .jobs-easy-apply-modal with phone/email inputs. dom-bayt.html has applyForm with PascalCase names and Arabic labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for all 3 platforms</name>
  <files>
    tests/unit/icims.test.js
    tests/unit/linkedin.test.js
    tests/unit/bayt.test.js
  </files>
  <action>
Create three test files using node:test with todo stubs. Pattern: self-contained mini DOM mock seeded from fixture HTML (same approach as lever.test.js — Phase 07-03 decision: document mock built per-test-file, not shared). Use `createRequire(__filename)` (not `import.meta.url` — CJS-only project per Phase 08 decision).

**tests/unit/icims.test.js** — 7 todo stubs covering:
1. `TEST: matches() returns true for icims.com hostname` (FR-2.1)
2. `TEST: matches() returns true for careers.icims.com hostname` (FR-2.1)
3. `TEST: matches() returns false for non-iCIMS hostname` (FR-2.1)
4. `TEST: fill() returns results array with filled status for standard fields` (FR-2.3)
5. `TEST: fill() skips fields that already have a value` (FR-2.3)
6. `TEST: fill() detects cross-origin iframe and returns failed result` (FR-7.2)
7. `TEST: getJobDetails() parses job title from iCIMS_JobTitle element` (FR-2.3)

Load fixture: `readFileSync(join(__dirname, '../fixtures/dom-icims.html'), 'utf8')`
Set up window/document mock seeded with fixture HTML before each test group.
Set up `window.JobFill.filler.fillField` stub returning true.
Set up `window.JobFill.matcher.findBestAnswer` stub returning null.

**tests/unit/linkedin.test.js** — 7 todo stubs covering:
1. `TEST: matches() returns true for linkedin.com hostname` (FR-2.2)
2. `TEST: fill() returns empty array when .jobs-easy-apply-modal not present` (FR-7.3 — isEasyApplyContext guard)
3. `TEST: fill() fills phone field inside modal` (FR-7.3)
4. `TEST: fill() fills firstName and lastName inside modal` (FR-2.3)
5. `TEST: fill() does not click Submit or Next buttons` (FR-7.3 — no auto-submit)
6. `TEST: fill() sets window._jobfillLinkedInObserver on modal` (FR-7.3 — MutationObserver stored)
7. `TEST: fill() skips fields that already have a value` (FR-2.3)

**tests/unit/bayt.test.js** — 6 todo stubs covering:
1. `TEST: matches() returns true for bayt.com hostname` (FR-2.2)
2. `TEST: fill() fills FirstName using name attribute selector` (FR-7.5)
3. `TEST: fill() fills Email using type=email selector` (FR-7.5)
4. `TEST: fill() does not use label text for field identification` (FR-7.5 — RTL safety)
5. `TEST: fill() returns skipped when isNativeBaytForm() returns false` (FR-7.5)
6. `TEST: fill() fills Phone using type=tel or name=Phone selector` (FR-7.5)

All 20 stubs must use `{ todo: 'implement platforms/PLATFORM.js' }` option so they show as passing with annotation (not failing). The module require must be wrapped in try/catch so stubs run even before the platform JS file exists.

Run `node tests/run-all.js` (or equivalent) after creation to confirm 0 failures.
  </action>
  <verify>node --test tests/unit/icims.test.js tests/unit/linkedin.test.js tests/unit/bayt.test.js 2>&1 | tail -5</verify>
  <done>All 20 stubs pass (shown as todo-annotated). Zero failures. Three test files exist with correct fixture loading, window.JobFill mock wiring, and todo anchors for all required behaviors.</done>
</task>

</tasks>

<verification>
node --test tests/unit/icims.test.js tests/unit/linkedin.test.js tests/unit/bayt.test.js
Expected: 20 pass (todo), 0 fail, 0 error
</verification>

<success_criteria>
- tests/fixtures/dom-icims.html, dom-linkedin.html, dom-bayt.html all exist
- tests/unit/icims.test.js has 7 todo stubs, linkedin.test.js has 7, bayt.test.js has 6
- node --test on all three files exits 0 (todo stubs count as passing)
- No external npm dependencies added (node:test + fs + path only)
</success_criteria>

<output>
After completion, create `.planning/phases/09-icims-linkedin-easy-apply-and-bayt-modules/09-01-SUMMARY.md`
</output>
