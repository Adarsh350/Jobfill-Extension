---
phase: 08-workday-and-ashby-platform-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/dom-workday.html
  - tests/fixtures/dom-ashby.html
  - tests/unit/workday.test.js
  - tests/unit/ashby.test.js
autonomous: true
requirements:
  - FR-2.1
  - FR-2.3
  - FR-2.6
  - FR-2.9
  - FR-7.1

must_haves:
  truths:
    - "node tests/unit/workday.test.js exits 0 with all stubs as todo (0 fail)"
    - "node tests/unit/ashby.test.js exits 0 with all stubs as todo (0 fail)"
    - "dom-workday.html contains shadow roots with data-automation-id inputs"
    - "dom-ashby.html contains [data-field-type] wrapped inputs and a React-style h1/title"
  artifacts:
    - path: "tests/fixtures/dom-workday.html"
      provides: "Shadow DOM fixture simulating Workday form fields"
      contains: "attachShadow"
    - path: "tests/fixtures/dom-ashby.html"
      provides: "Standard DOM fixture simulating Ashby React form"
      contains: "data-field-type"
    - path: "tests/unit/workday.test.js"
      provides: "7 todo test stubs covering FR-2.1, FR-2.3, FR-2.6, FR-7.1"
      exports: []
    - path: "tests/unit/ashby.test.js"
      provides: "6 todo test stubs covering FR-2.1, FR-2.3, FR-2.6, FR-2.9"
      exports: []
  key_links:
    - from: "tests/unit/workday.test.js"
      to: "tests/fixtures/dom-workday.html"
      via: "fs.readFileSync + manual shadow attachment in test setup"
      pattern: "dom-workday"
    - from: "tests/unit/ashby.test.js"
      to: "tests/fixtures/dom-ashby.html"
      via: "fs.readFileSync or inline DOM mock"
      pattern: "dom-ashby"
---

<objective>
Create the Wave 1 test scaffold for Phase 8: DOM fixtures and todo stub test files for both Workday and Ashby.

Purpose: Establish the RED baseline before any implementation. Wave 2 plans (08-02, 08-03) depend on these stubs existing and passing as todo.
Output: 4 files — 2 HTML fixtures, 2 test files. All stubs pass as todo, 0 fail.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workday-and-ashby-platform-modules/08-RESEARCH.md

<interfaces>
<!-- Phase 7 test pattern — established precedent for this scaffold -->
<!-- From tests/unit/greenhouse.test.js and tests/unit/lever.test.js (Phase 7-01) -->

Test file structure (node:test, no npm, no jsdom):
```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

// Self-contained DOM mock built inside test file
// window.JobFill.platforms.workday required at top via createRequire or try/catch

describe('workday', function () {
  it('TEST: matches myworkdayjobs.com', { todo: true }, function () {});
  // ... more todo stubs
});
```

Key constraint: No jsdom, no npm. Shadow DOM must be simulated using attachShadow in the HTML
fixture script tag, OR faked in tests by patching shadowRoot on host elements.

From utils/events.js — confirmed available:
  window.JobFill.events.dispatchBlur(el)  // line 14-16

From utils/filler.js — shadowQuery signature:
  window.JobFill.filler.shadowQuery(root, selector)  // recursive shadow walk
  window.JobFill.filler.shadowQueryAll(root, selector)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DOM fixtures (dom-workday.html, dom-ashby.html)</name>
  <files>tests/fixtures/dom-workday.html, tests/fixtures/dom-ashby.html</files>
  <action>
Create tests/fixtures/dom-workday.html following the structure from research RESEARCH.md "DOM Fixture Pattern" section. The fixture must:
- Have a `<div id="workday-app">` container
- Create shadow hosts for each field section: legalName, email, phone, address, country, linkedin, jobTitle
- Use inline `<script>` to call `attachShadow({ mode: 'open' })` on each host
- Inject inputs with `data-automation-id` matching the FIELDS table in RESEARCH.md:
  - `legalNameSection_firstName` (text input)
  - `legalNameSection_lastName` (text input)
  - `email` (email input)
  - `phone-number` (tel input)
  - `addressSection_city` (text input)
  - `country` (select with options: "", "United Arab Emirates", "India", "United States")
  - `linkedIn` containing input (text input, data-automation-id="linkedIn")
  - `h2[data-automation-id="jobPostingHeader"]` with text "Senior Product Marketing Manager"
- Include a second-step hidden section div with `style="display:none"` containing a text input — used to test the isVisible guard

Create tests/fixtures/dom-ashby.html with:
- `<title>Senior Product Marketing Manager at Acme Corp | Ashby</title>`
- `<h1>Senior Product Marketing Manager</h1>`
- Standard React-style inputs (no shadow DOM):
  - `input[name="firstName"]` inside a label
  - `input[name="lastName"]` inside a label
  - `input[type="email"]` inside a label
  - `input[type="tel"]` inside a label
  - `input[name="linkedin"]` inside a label
- Two custom question fields using `[data-field-type]`:
  - `<div data-field-type="longText"><label>Why do you want to work here?</label><textarea></textarea></div>`
  - `<div data-field-type="shortText"><label>Work authorization status?</label><input type="text"></div>`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const w=fs.readFileSync('tests/fixtures/dom-workday.html','utf8'); const a=fs.readFileSync('tests/fixtures/dom-ashby.html','utf8'); console.assert(w.includes('attachShadow'),'workday missing attachShadow'); console.assert(w.includes('legalNameSection_firstName'),'workday missing firstName'); console.assert(a.includes('data-field-type'),'ashby missing data-field-type'); console.assert(a.includes('Acme Corp'),'ashby missing title'); console.log('fixtures OK')"</automated>
  </verify>
  <done>Both HTML files exist. dom-workday.html has attachShadow calls and data-automation-id inputs. dom-ashby.html has data-field-type custom question wrappers and parseable title.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files (workday.test.js, ashby.test.js)</name>
  <files>tests/unit/workday.test.js, tests/unit/ashby.test.js</files>
  <action>
Create tests/unit/workday.test.js with 7 todo stubs following Phase 7 precedent (node:test, self-contained DOM mock, no jsdom). The file must:
- Use `import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'`
- Build a minimal self-contained DOM mock (document object with querySelector/querySelectorAll returning null, window.JobFill stub namespace)
- Patch window.JobFill.filler.shadowQuery and shadowQueryAll as stubs returning null
- Patch window.JobFill.events.dispatchBlur as a no-op stub, tracking call count
- Load platforms/workday.js via createRequire with try/catch (file does not yet exist — catch and skip module load, tests still register as todo)
- Include these 7 todo stubs:
  1. `TEST: matches returns true for myworkdayjobs.com hostname`
  2. `TEST: matches returns false for greenhouse.io`
  3. `TEST: fill resolves firstName from shadow DOM fixture`
  4. `TEST: fill skips field if already has value`
  5. `TEST: fill skips field if not visible (isVisible guard)`
  6. `TEST: fill calls dispatchBlur after each filled field`
  7. `TEST: getJobDetails returns jobTitle from jobPostingHeader and companyName from subdomain`

Create tests/unit/ashby.test.js with 6 todo stubs:
- Same import pattern (node:test, assert/strict)
- Minimal DOM mock with document.querySelector returning null, document.querySelectorAll returning []
- document.title stub set to "Senior PM at Acme | Ashby", document.querySelector('h1') returning { textContent: 'Senior PM' }
- Load platforms/ashby.js via createRequire with try/catch
- Include these 6 todo stubs:
  1. `TEST: matches returns true for ashbyhq.com hostname`
  2. `TEST: matches returns false for greenhouse.io`
  3. `TEST: fill fills firstName with fillField`
  4. `TEST: fill skips already-filled fields`
  5. `TEST: fill scans [data-field-type] custom questions`
  6. `TEST: getJobDetails extracts company from title and job from h1`

Both files must exit 0 when run with node. Use `{ todo: true }` on all it() stubs.
  </action>
  <verify>
    <automated>node tests/unit/workday.test.js && node tests/unit/ashby.test.js</automated>
  </verify>
  <done>Both test files run with 0 failures. All stubs appear as todo. node tests/unit/workday.test.js and node tests/unit/ashby.test.js both exit 0.</done>
</task>

</tasks>

<verification>
node tests/unit/workday.test.js && node tests/unit/ashby.test.js — both exit 0, all stubs todo, 0 fail
node tests/run-all.js — full suite still passes (no regressions)
</verification>

<success_criteria>
- tests/fixtures/dom-workday.html exists with shadow DOM structure and data-automation-id inputs
- tests/fixtures/dom-ashby.html exists with [data-field-type] custom question wrappers
- tests/unit/workday.test.js has 7 todo stubs, exits 0
- tests/unit/ashby.test.js has 6 todo stubs, exits 0
- Full test suite (node tests/run-all.js) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-workday-and-ashby-platform-modules/08-01-SUMMARY.md`
</output>
