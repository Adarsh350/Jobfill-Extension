---
phase: 06-fill-status-overlay
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/overlay.js
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.5
  - FR-4.6
  - NFR-4.4

must_haves:
  truths:
    - "Overlay host element mounts inside Shadow DOM — ATS page styles cannot reach overlay elements"
    - "Fill button appears bottom-right on any ATS page (indigo, '✨ Fill Form')"
    - "showResults() renders one row per fill result with correct colour dot: green/yellow/red/orange"
    - "Overlay header is draggable; position persists across tab reload via chrome.storage.sync settings"
    - "X button dismisses overlay, removes host from DOM, and disconnects the MutationObserver"
    - "showBanner() renders an inline warning strip that does not crash if chrome.runtime is invalid"
    - "Shadow root uses _container div pattern — innerHTML clears never wipe the injected style tag"
  artifacts:
    - path: "utils/overlay.js"
      provides: "window.JobFill.overlay IIFE with Shadow DOM, showButton, showResults, showBanner, dismiss, setObserverRef"
      contains: "attachShadow"
  key_links:
    - from: "utils/overlay.js _ensureHost()"
      to: "document.body"
      via: "appendChild(_host) then _host.attachShadow({ mode: 'open' })"
      pattern: "attachShadow"
    - from: "utils/overlay.js drag handler onUp"
      to: "window.JobFill.storage.saveSettings()"
      via: "mouseup → getSettings().then(s => saveSettings({ ...s, overlayRight, overlayBottom }))"
      pattern: "saveSettings"
    - from: "utils/overlay.js dismiss()"
      to: "_obs.disconnect()"
      via: "setObserverRef stores observer; dismiss() calls _obs.disconnect() before nulling _host"
      pattern: "disconnect"
---

<objective>
Implement utils/overlay.js as the Shadow DOM floating UI module. Exports window.JobFill.overlay with
showButton(), showResults(), showBanner(), dismiss(), and setObserverRef(). Uses the _container div
pattern so style tags survive innerHTML clears.

Purpose: Provides the visual feedback layer for all fill operations. Without this, there is no UI
presence on the job page and no way to show results to the user.

Output: A complete utils/overlay.js IIFE that is self-contained and callable from content.js.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-fill-status-overlay/06-RESEARCH.md

<interfaces>
<!-- Key APIs the executor needs. Read directly from source files. -->

window.JobFill.storage (from utils/storage.js):
  getSettings()                    → Promise<object>     // may return {} if unset
  saveSettings(settings)           → Promise<void>       // merges/replaces settings key

chrome.runtime.sendMessage usage in overlay (FR-4.4 needs-review click):
  chrome.runtime.sendMessage({ type: 'OPEN_ANSWER_BANK', id: entry.id })
  // Must be wrapped in try/catch (NFR-4.4) — context may be invalidated

Fill result object shape (one entry per field):
  { field: string, status: 'filled'|'skipped'|'failed'|'needs_review', value?: string, reason?: string }

Status colour mapping:
  filled       → #22c55e  (green)
  skipped      → #eab308  (yellow)
  failed       → #ef4444  (red)
  needs_review → #f97316  (orange)

content.js calls overlay as:
  window.JobFill.overlay.showButton(runFill)    // runFill is an async function → Promise
  window.JobFill.overlay.showResults(results)   // results is Fill result[]
  window.JobFill.overlay.showBanner(message)    // message is a string
  window.JobFill.overlay.setObserverRef(obs)    // obs is a MutationObserver instance
  window.JobFill.overlay.dismiss()              // no args
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement utils/overlay.js — Shadow DOM host + showButton</name>
  <files>utils/overlay.js</files>
  <action>
Replace the 3-line stub with the complete IIFE. Implement in this order:

1. Namespace guard + IIFE open:
   window.JobFill = window.JobFill || {};
   window.JobFill.overlay = (function() { 'use strict'; ... })();

2. Private state:
   let _host = null;     // the div#jobfill-overlay-host appended to document.body
   let _shadow = null;   // the ShadowRoot (mode: 'open')
   let _container = null; // div inside shadow — only this gets innerHTML-cleared
   let _obs = null;      // MutationObserver reference for disconnect on dismiss

3. STATUS_COLORS constant object:
   const STATUS_COLORS = { filled: '#22c55e', skipped: '#eab308', failed: '#ef4444', needs_review: '#f97316' };

4. _ensureHost() — idempotent setup:
   a. If _host is not null, return immediately (already mounted)
   b. Create _host = document.createElement('div'); _host.id = 'jobfill-overlay-host'
   c. Set _host.style.cssText — position:fixed; bottom:16px; right:16px; z-index:2147483647; font-family:system-ui,sans-serif
   d. Append to document.body
   e. _shadow = _host.attachShadow({ mode: 'open' })
   f. Create <style> element, inject ALL overlay CSS into its textContent (see CSS spec below), append to _shadow
   g. Create _container = document.createElement('div'); _container.className = 'jf-root'
   h. Append _container to _shadow
   IMPORTANT: style tag is appended to _shadow directly. _container is appended after. Only _container
   gets cleared — never _shadow.innerHTML = '' (would destroy the style tag).

   CSS to inject into the style tag (all scoped to shadow DOM — no external leakage):
   ```
   *, *::before, *::after { box-sizing: border-box; }
   .jf-panel { background:#fff; border:1px solid #e5e7eb; border-radius:8px;
               box-shadow:0 4px 12px rgba(0,0,0,.15); min-width:280px; max-width:360px; overflow:hidden; }
   .jf-header { display:flex; justify-content:space-between; align-items:center;
                padding:8px 12px; background:#6366f1; color:#fff; border-radius:8px 8px 0 0;
                cursor:move; user-select:none; font-size:13px; font-weight:600; }
   .jf-close { background:none; border:none; color:#fff; cursor:pointer; font-size:18px;
               padding:0 0 0 8px; line-height:1; }
   .jf-body { padding:4px 0; max-height:300px; overflow-y:auto; }
   .jf-row { display:flex; gap:8px; padding:6px 12px; border-bottom:1px solid #f3f4f6;
             font-size:12px; align-items:flex-start; }
   .jf-row:last-child { border-bottom:none; }
   .jf-dot { width:8px; height:8px; border-radius:50%; flex-shrink:0; margin-top:3px; }
   .jf-field { font-weight:600; flex-shrink:0; min-width:80px; }
   .jf-value { color:#6b7280; word-break:break-word; }
   .jf-row-clickable { cursor:pointer; }
   .jf-row-clickable:hover { background:#fff7ed; }
   .jf-btn { display:block; width:calc(100% - 24px); margin:10px 12px;
             padding:9px 0; background:#6366f1; color:#fff; border:none; border-radius:6px;
             font-size:14px; font-weight:600; cursor:pointer; text-align:center; }
   .jf-btn:hover { background:#4f46e5; }
   .jf-banner { padding:8px 12px; background:#fef3c7; color:#92400e; font-size:12px;
                border-radius:8px; }
   ```

5. _makeDraggable(headerEl) — drag implementation:
   headerEl.addEventListener('mousedown', function(e) {
     e.preventDefault();
     const startX = e.clientX, startY = e.clientY;
     const origRight  = parseInt(_host.style.right)  || 16;
     const origBottom = parseInt(_host.style.bottom) || 16;
     function onMove(ev) {
       _host.style.right  = Math.max(0, origRight  - (ev.clientX - startX)) + 'px';
       _host.style.bottom = Math.max(0, origBottom + (ev.clientY - startY)) + 'px';
     }
     function onUp() {
       document.removeEventListener('mousemove', onMove);
       document.removeEventListener('mouseup', onUp);
       // Persist position — named functions required for reliable removeEventListener
       window.JobFill.storage.getSettings().then(function(s) {
         s = s || {};
         s.overlayRight  = _host.style.right;
         s.overlayBottom = _host.style.bottom;
         return window.JobFill.storage.saveSettings(s);
       }).catch(function(err) { console.warn('[JobFill overlay]', err.message); });
     }
     document.addEventListener('mousemove', onMove);
     document.addEventListener('mouseup', onUp);
   });

6. showButton(onFill) function:
   a. _ensureHost()
   b. If _host already showing results panel (check _container.querySelector('.jf-panel')), do nothing — let user dismiss first
   c. _container.innerHTML = '' (safe — only clears container, not shadow root)
   d. Create panel div.jf-panel
   e. Create header div.jf-header with text '✨ Fill Form' and close button (×)
   f. Close button onclick: dismiss()
   g. _makeDraggable(header)
   h. Create button div.jf-btn with text '✨ Fill Form'
   i. Button onclick: calls onFill() — onFill is the runFill async fn from content.js
   j. Append header + button to panel; append panel to _container
   k. Load saved position from settings (async, non-blocking):
      window.JobFill.storage.getSettings().then(function(s) {
        if (!_host) return; // dismissed before load completed
        if (s && s.overlayRight)  _host.style.right  = s.overlayRight;
        if (s && s.overlayBottom) _host.style.bottom = s.overlayBottom;
      }).catch(function() {});

7. setObserverRef(obs):
   function setObserverRef(obs) { _obs = obs; }

8. dismiss() function:
   a. If _obs: _obs.disconnect(); _obs = null
   b. If _host: _host.remove(); _host = null; _shadow = null; _container = null

9. Return public API:
   return { showButton, showResults, showBanner, dismiss, setObserverRef };
   (showResults and showBanner implemented in Task 2 — stubs here returning undefined are acceptable
    as long as they are exported and the module does not throw on load)
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('C:/Users/JobSearch/Documents/Projects/utils/overlay.js', 'utf8');
const checks = [
  ['attachShadow present', /attachShadow/.test(src)],
  ['_container pattern (no shadow innerHTML clear)', /_container/.test(src) && !/shadow\.innerHTML/.test(src)],
  ['host id jobfill-overlay-host', /jobfill-overlay-host/.test(src)],
  ['z-index max', /2147483647/.test(src)],
  ['STATUS_COLORS defined', /STATUS_COLORS/.test(src)],
  ['showButton exported', /showButton/.test(src)],
  ['dismiss exported', /dismiss/.test(src)],
  ['setObserverRef exported', /setObserverRef/.test(src)],
  ['drag mousedown handler', /mousedown/.test(src)],
  ['drag cleanup removeEventListener', /removeEventListener.*mousemove|removeEventListener.*onMove/.test(src)],
];
let pass = true;
checks.forEach(([name, ok]) => { console.log((ok ? 'PASS' : 'FAIL') + ': ' + name); if (!ok) pass = false; });
process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <done>
All 10 checks pass. utils/overlay.js has Shadow DOM host with _container pattern, z-index max,
showButton with draggable header, dismiss with observer disconnect, setObserverRef, and STATUS_COLORS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement showResults() and showBanner() in utils/overlay.js</name>
  <files>utils/overlay.js</files>
  <action>
Add showResults() and showBanner() implementations inside the existing IIFE, before the return statement.
Do NOT rewrite the file — Read it first, then Edit to insert the two functions.

showResults(results) implementation:
  a. _ensureHost()
  b. _container.innerHTML = '' (clear previous content — safe, container only)
  c. Create panel div.jf-panel
  d. Create header div.jf-header:
     - Left text: '✨ JobFill Results'
     - Right: count summary span — e.g. "3 filled · 1 skipped"
       Build count summary: count items per status, show only non-zero counts joined by ' · '
     - Close button (×): onclick calls dismiss()
     - _makeDraggable(header)
  e. Create body div.jf-body
  f. For each result in results array, create div.jf-row:
     - Create dot span.jf-dot with background color STATUS_COLORS[result.status] || '#9ca3af'
     - Create field span.jf-field with text result.field
     - Create value span.jf-value:
         if result.value: text = result.value (truncate to 40 chars with '…' if longer)
         else if result.reason: text = result.reason
         else: text = result.status
     - If result.status === 'needs_review':
         Add class 'jf-row-clickable' to the row
         row.addEventListener('click', function() {
           try {
             chrome.runtime.sendMessage({ type: 'OPEN_ANSWER_BANK', id: result.id || result.field });
           } catch(err) {
             console.warn('[JobFill overlay]', err.message);
           }
         });
     - Append dot + field + value to row; append row to body
  g. Append header + body to panel; append panel to _container
  h. Load saved position (same async non-blocking pattern as showButton)

showBanner(message) implementation:
  a. _ensureHost()
  b. _container.innerHTML = ''
  c. Create div.jf-banner with textContent = message
  d. Append to _container
  e. No auto-dismiss — stays until user navigates away or reloads
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('C:/Users/JobSearch/Documents/Projects/utils/overlay.js', 'utf8');
const checks = [
  ['showResults implemented (not stub)', /function showResults/.test(src) && src.split('function showResults')[1].length > 100],
  ['showBanner implemented (not stub)', /function showBanner/.test(src) && src.split('function showBanner')[1].length > 50],
  ['STATUS_COLORS used in showResults', /STATUS_COLORS\[/.test(src)],
  ['needs_review clickable', /needs_review/.test(src)],
  ['OPEN_ANSWER_BANK message', /OPEN_ANSWER_BANK/.test(src)],
  ['chrome.runtime sendMessage try/catch (NFR-4.4)', /try[\s\S]{0,80}sendMessage[\s\S]{0,80}catch/.test(src)],
  ['count summary in showResults', /filled|skipped|failed/.test(src.split('showResults')[1] || '')],
  ['jf-banner class used', /jf-banner/.test(src)],
];
let pass = true;
checks.forEach(([name, ok]) => { console.log((ok ? 'PASS' : 'FAIL') + ': ' + name); if (!ok) pass = false; });
process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <done>
All 8 checks pass. showResults renders colour-coded rows with count summary, needs_review rows are
clickable with OPEN_ANSWER_BANK wrapped in try/catch. showBanner renders the invalidated context strip.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run combined validation:
  node -e "..." (both check scripts above concatenated)
Manual smoke test: Load extension on any page; check DevTools Elements for
  #jobfill-overlay-host > #shadow-root (open) > style + div.jf-root
</verification>

<success_criteria>
- utils/overlay.js is a complete IIFE exporting 5 public functions
- Shadow DOM host uses _container pattern — style tag survives all innerHTML clears
- Fill button visible bottom-right with indigo colour and '✨ Fill Form' text
- showResults renders filled(green)/skipped(yellow)/failed(red)/needs_review(orange) rows
- Dragging header updates position; position persists across reload via saveSettings
- X button removes host from DOM and disconnects MutationObserver via _obs.disconnect()
- All chrome.runtime.sendMessage calls wrapped in try/catch (NFR-4.4)
- Combined node validation: all 18 checks (10 from Task 1 + 8 from Task 2) PASS
</success_criteria>

<output>
After completion, create `.planning/phases/06-fill-status-overlay/06-02-SUMMARY.md`
</output>
