---
phase: 07-greenhouse-and-lever-platform-modules
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/dom-greenhouse.html
  - tests/fixtures/dom-lever.html
  - tests/unit/greenhouse.test.js
  - tests/unit/lever.test.js
autonomous: true
requirements:
  - FR-2.1
  - FR-2.3
  - FR-2.4
  - FR-2.6
  - FR-2.9
  - FR-3.4
  - FR-3.5
  - FR-3.7
  - FR-7.4
  - NFR-5.4

must_haves:
  truths:
    - "greenhouse.test.js runs with node --test and exits 0 (all stubs present)"
    - "lever.test.js runs with node --test and exits 0 (all stubs present)"
    - "All test cases are todo stubs — none pass implementation yet"
    - "DOM fixtures contain the exact HTML field structures each test will query"
  artifacts:
    - path: "tests/fixtures/dom-greenhouse.html"
      provides: "Static Greenhouse form HTML for DOM mock tests"
      contains: "first_name, last_name, email, phone, linkedin, cover_letter, work_auth, custom textarea"
    - path: "tests/fixtures/dom-lever.html"
      provides: "Static Lever form HTML for DOM mock tests"
      contains: "input[name=name], email, phone, urls[LinkedIn], comments, data-qa custom question"
    - path: "tests/unit/greenhouse.test.js"
      provides: "10 todo-stub test cases for Greenhouse module"
      exports: []
    - path: "tests/unit/lever.test.js"
      provides: "8 todo-stub test cases for Lever module"
      exports: []
  key_links:
    - from: "tests/unit/greenhouse.test.js"
      to: "tests/helpers/dom-mock.js"
      via: "require('../helpers/dom-mock')"
      pattern: "require.*dom-mock"
    - from: "tests/unit/lever.test.js"
      to: "tests/helpers/dom-mock.js"
      via: "require('../helpers/dom-mock')"
      pattern: "require.*dom-mock"
---

<objective>
Wave 0 TDD scaffold: create DOM fixture HTML files and test files with todo-stub test cases for both Greenhouse and Lever platform modules. No implementation yet — only test structure that will go RED → GREEN in plan 07-02 and 07-03.

Purpose: Establish the test contract before implementation. Mirrors the Phase 4 and Phase 5 Wave 0 pattern already used in this project.
Output: 4 files — 2 HTML fixtures, 2 test files with todo stubs. `node --test tests/unit/greenhouse.test.js && node --test tests/unit/lever.test.js` exits 0.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-greenhouse-and-lever-platform-modules/07-RESEARCH.md

<interfaces>
<!-- Reuse pattern from Phase 4 Wave 0 — already established in this codebase -->

From tests/helpers/dom-mock.js (Phase 4, already exists):
- Provides Node.js DOM shim (document.querySelector, createElement, etc.)
- Provides nativeSetterSpy for testing React-compatible event dispatch
- Pattern: require('../helpers/dom-mock') at top of test file

From tests/unit/events.test.js (Phase 4 pattern to follow):
- Uses node:test built-in: const { test, describe } = require('node:test')
- Todo stubs: test('description', { todo: 'not implemented' }, () => {})
- No jest, no vitest — NFR-1.2 zero build tools

Module under test load pattern (from Phase 5):
  try {
    require('../../platforms/greenhouse');
    gh = window.JobFill.platforms.greenhouse;
  } catch (e) { gh = null; }
  // tests use: if (!gh) { ... todo ... }

Platform module contract (from content.js + RESEARCH.md):
  window.JobFill.platforms.greenhouse = { matches, fill, getJobDetails }
  fill(profile, answerBank) returns Array<{ field, status, value?, reason? }>
  status values: 'filled' | 'skipped' | 'failed' | 'needs_review'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DOM fixture HTML files</name>
  <files>tests/fixtures/dom-greenhouse.html, tests/fixtures/dom-lever.html</files>
  <action>
Create two static HTML fixture files. These are loaded by the DOM mock helper in tests to provide realistic field structures.

**tests/fixtures/dom-greenhouse.html** — must contain:
- `<input id="first_name" name="first_name" type="text">`
- `<input id="last_name" name="last_name" type="text">`
- `<input id="email" name="email" type="email">`
- `<input id="phone" name="phone" type="tel">`
- `<input id="job_application_linkedin_profile" name="linkedin" type="text">`
- `<input id="resume" name="resume" type="file">`
- `<textarea id="cover_letter" name="cover_letter"></textarea>`
- `<select id="job_application_location" name="location"><option value="">Select</option><option value="Dubai">Dubai</option><option value="Abu Dhabi">Abu Dhabi</option></select>`
- `<select name="work_auth_question"><option value="">Select</option><option value="Yes">Yes, authorized</option><option value="No">No</option></select>`
- A custom question div: `<div class="field"><label for="custom_q1">Why do you want this role?</label><textarea id="custom_q1"></textarea></div>`
- `<h1 class="app-title">Senior Product Manager</h1>`
- `<h1 class="company-name">Acme Corp</h1>`

**tests/fixtures/dom-lever.html** — must contain:
- `<input name="name" type="text">`
- `<input name="email" type="email">`
- `<input name="phone" type="tel">`
- `<input name="urls[LinkedIn]" type="text">`
- `<input name="resume" type="file">`
- `<textarea name="comments" placeholder="Cover letter"></textarea>`
- `<input name="urls[Portfolio]" type="text">`
- A custom question via data-qa: `<div data-qa="additional-cards-question"><label>Why are you interested?</label><textarea></textarea></div>`
- `<h2>Product Marketing Manager</h2>`
- `<div class="main-header-logo"><img alt="Acme Corp" src="logo.png"></div>`

Both files are minimal valid HTML5 documents (DOCTYPE, html, head, body). No CSS or JS needed — just the structural HTML.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['tests/fixtures/dom-greenhouse.html','tests/fixtures/dom-lever.html'].forEach(f=>{ if(!fs.existsSync(f)) throw new Error('Missing: '+f); const c=fs.readFileSync(f,'utf8'); if(!c.includes('DOCTYPE')) throw new Error('Not valid HTML: '+f); }); console.log('fixtures ok')"</automated>
  </verify>
  <done>Both HTML fixture files exist, contain the specified field structures, and are valid HTML5 documents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create greenhouse.test.js and lever.test.js with todo stubs</name>
  <files>tests/unit/greenhouse.test.js, tests/unit/lever.test.js</files>
  <behavior>
    Greenhouse test stubs (10 total):
    - TEST: greenhouse matches greenhouse.io hostname → true
    - TEST: greenhouse does not match lever.co hostname → false
    - TEST: greenhouse exposes matches, fill, getJobDetails functions
    - TEST: greenhouse fill — first name filled from profile
    - TEST: greenhouse fill — email filled from profile
    - TEST: greenhouse fill — file input skipped with reason 'resume upload in Phase 11'
    - TEST: greenhouse fill — already-filled field skipped with reason 'already has value'
    - TEST: greenhouse fill — fallback selector used when primary not found
    - TEST: greenhouse fill — custom question filled from answer bank (confidence >= 0.75)
    - TEST: greenhouse fill — needs_review when answer has unresolved variables

    Lever test stubs (8 total):
    - TEST: lever matches lever.co hostname → true
    - TEST: lever does not match greenhouse.io hostname → false
    - TEST: lever exposes matches, fill, getJobDetails functions
    - TEST: lever fill — full name filled from profile
    - TEST: lever fill — email filled from profile
    - TEST: lever fill — file input skipped with reason 'resume upload in Phase 11'
    - TEST: lever fill — already-filled field skipped
    - TEST: lever fill — data-qa custom question filled from answer bank
  </behavior>
  <action>
Follow the exact same todo-stub pattern used in tests/unit/events.test.js (Phase 4). Use node:test built-in only.

**Pattern to follow exactly:**
```javascript
'use strict';
const { test, describe } = require('node:test');
require('../helpers/dom-mock');

// Load module under test (may not exist yet)
let gh = null;
try {
  require('../../platforms/greenhouse');
  gh = (window.JobFill && window.JobFill.platforms && window.JobFill.platforms.greenhouse) || null;
} catch (e) { /* module not yet implemented */ }

describe('greenhouse', () => {
  test('TEST: greenhouse matches greenhouse.io hostname → true', { todo: 'not implemented' }, () => {});
  // ... remaining stubs
});
```

For lever.test.js: same pattern, load `platforms/lever`, use `window.JobFill.platforms.lever`.

All 18 stubs across both files must have the prefix "TEST: " in the description (matches the Phase 4 convention for grep-ability). All use `{ todo: 'not implemented' }`. None have implementation bodies.

Do NOT add any passing assertions — Wave 0 is stubs only.
  </action>
  <verify>
    <automated>node --test tests/unit/greenhouse.test.js && node --test tests/unit/lever.test.js</automated>
  </verify>
  <done>Both test files run with node --test and exit 0. All 18 stubs show as todo. No failures. Full suite `node --test tests/unit/` still exits 0.</done>
</task>

</tasks>

<verification>
Run full test suite: `node --test tests/unit/`
Confirm both new test files appear in output with all stubs marked todo.
Confirm no existing tests regressed.
</verification>

<success_criteria>
- tests/fixtures/dom-greenhouse.html exists with all 12 required field structures
- tests/fixtures/dom-lever.html exists with all 9 required field structures
- tests/unit/greenhouse.test.js has exactly 10 todo stubs, exits 0
- tests/unit/lever.test.js has exactly 8 todo stubs, exits 0
- `node --test tests/unit/` exits 0 (full suite green)
</success_criteria>

<output>
After completion, create `.planning/phases/07-greenhouse-and-lever-platform-modules/07-01-SUMMARY.md`
</output>
